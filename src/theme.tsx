import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#b49da6',
      main: '#683b4d',
    },
    secondary: {
      main: '#2d7a8a',
    },
    success: {
      main: '#2a976d',
    },
  },
});

export default theme;
